# Acquire users first and last name. Flip the names and print input.
# Acquire inputs from user
f_name = input("What is your first name: ")
l_name = input("What is your last name: ")

# Output users last and first name in specific order
print(l_name + " " + f_name)
